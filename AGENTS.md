# AGENTS.md · AI 悬浮桌面宠物 工作指引

> 本文件是项目协作的**入口指引**。任何参与本项目开发的 Agent 或协作者，开始工作前必须先阅读本文件与对应规范文档。

---

## 1. 项目概览

**AI 悬浮桌面宠物**：基于 Electron 的 Windows 桌面悬浮宠物，包含——① 提醒功能（定点/周期/使用时长）② 常用命令记录器（右键打开居中配置中心独立窗口全量管理，置顶排序 + 点击复制）③ 双击交互面板（对话气泡 + 输入框，v2.0 取代复制看板，v2.1 双击/右键对调，**v2.3 接入大模型流式对话**：chat.js 后端 + .env 填 `DEEPSEEK_API_KEY` 即用）。项目用于 vibe coding 课程演示。

## 2. 标准文件路径索引

| 文档 | 路径 | 用途 | 何时读 |
|---|---|---|---|
| 开发前问题清单 | `docs/开发前问题清单.md` | 18 项决策点及备选项，记录用户确认状态 | 决策有争议时 |
| 需求规范 | `docs/需求规范.md` | 功能范围、交互框架、验收总标准 | **每个里程碑开始前必读** |
| 技术规范 | `docs/技术规范.md` | 技术选型、工程结构、数据模型、IPC 约定、安全红线 | 写代码前必读 |
| 设计规范 | `docs/设计规范.md` | 视觉风格、宠物状态机、气泡 UI、动效、素材规格 | 做 UI/素材时必读 |
| 开发步骤 | `docs/开发步骤.md` | M0-M6 里程碑、推进总规则、验收标准、时间预算 | **每次开工前必读，确认当前里程碑** |
| 开发日志 | `开发日志/开发日志.md` | 已完成/进行中/待办事项、变更记录、风险记录 | 每次开工先读，收工必写 |

## 3. 工作流程（强制）

```
开工 → 读《开发步骤》确认当前里程碑 → 读对应规范
     → 实施（小步验证）→ npm start 实测
     → 通过 → 更新《开发日志》（已完成/待办/变更记录）
     → 进入下一里程碑
```

关键规则：
1. **单步推进**：一次只做一个里程碑，禁止并行、禁止跳步
2. **一步一验**：每步必须 `npm start` 实测通过才算完成
3. **日志强制**：完成事项、待办变化、遇到的问题必须同步写入 `开发日志/开发日志.md`
4. **三次失败回退**：同一问题连续 3 次尝试失败，回退到上一可用状态，重新拆解并记录
5. **规范冲突先改规范**：实现与规范冲突时，先修订规范文档再写代码，保持文档与代码一致

## 4. 范围红线

- 已实现：提醒功能、常用命令记录器（右键打开居中配置中心独立窗口全量管理 + 置顶排序 + 点击复制，详见需求规范 §2）、双击交互面板（对话气泡 + 输入框，v2.0 取代复制看板；**v2.3 起发送消息由大模型流式回复**——主进程 chat.js 引擎，多轮历史/思考区/摘要压缩/错误归一，详见技术规范 §5.5）
- 不做：打开文件、命令执行、系统通知、错过补偿、agent 工具调用（见需求规范 §2.3 与 §5）
- 安全底线：`contextIsolation: true`、不执行用户命令、用户输入用 `textContent` 渲染；**API Key 只存主进程/.env（.env 被 .gitignore 与 files 白名单双重排除），渲染层不持有真实 Key**（详见技术规范 §7）
- **`deepseek-harness/` 仅为大模型开发参考资料**：不是本项目代码，其内容不构成本项目的规范或约束；除非用户明确指定参考该项目，否则不读取该文件夹中的代码

## 5. 目录结构

```
projects/
├── AGENTS.md          ← 本文件（工作指引入口）
├── docs/              ← 规范文档（需求/技术/设计/步骤/问题清单）
├── 开发日志/           ← 开发日志（自动维护）
├── package.json       ← 项目配置（electron + electron-store）
├── main.js            ← 主进程：窗口/IPC/数据/提醒调度器/会话引擎接入
├── chat.js            ← 大模型流式会话后端（v2.3：SSE 解析/历史压缩/错误归一）
├── .env               ← 大模型配置（git 忽略、不进包）：DEEPSEEK_API_KEY 等
├── preload.js         ← IPC 桥接（白名单接口）
├── index.html         ← 渲染层结构：宠物/气泡/配置中心
├── styles.css         ← 样式与动画
└── renderer.js        ← 渲染层逻辑：状态机/交互/CRUD
```
