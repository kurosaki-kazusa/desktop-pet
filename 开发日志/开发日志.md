# 开发日志 · AI 悬浮桌面宠物

> 本文件由开发过程**自动维护**：每完成一个里程碑或重要事项，必须立即更新本文件的"已完成事项"与"待办事项"，并在"变更记录"追加一行。
> 规则详见 [AGENTS.md](../AGENTS.md) 第 3 节。

---

## 📌 当前状态

- **当前里程碑**：「萤光织界」聊天/配置视觉升级（实现完成，v2.9，待本机视觉确认）
- **整体进度**：一期 M0-M6 全部完成 ✅ + 配置中心完成 ✅ + 复制看板/居中大窗/搜索完成 ✅ + 独立窗口改造完成 ✅ + 打包发布完成 ✅ + 性能优化完成 ✅ + 纯净版 v1.0.1 完成 ✅ + 交互面板改造完成 ✅（v2.0）+ 交互入口对调完成 ✅（v2.1）+ 一键打包脚本完成 ✅（v2.2）+ 大模型会话接入完成 ✅（v2.3）+ 性能与置顶优化完成 ✅（v2.4）+ 配置文件机制完成 ✅（v2.5）+ 默认图标与提醒预置调整完成 ✅（v2.7）+ 透明聊天头像替换完成 ✅（v2.8）+ 「萤光织界」视觉升级实现完成 ✅（v2.9 / app 2.2.3）
- **阻塞问题**：Codex 受管运行环境缺少可用 Electron GPU 子进程（`0xC0000135`），无法在本轮完成真实窗口视觉确认；需在正常 Windows 桌面环境运行 `npm start` 复验。

---

## ✅ 已完成事项

| 日期 | 事项 | 说明 |
|---|---|---|
| 2026-08-17 | 需求与技术选型确认 | 确定 Electron 技术栈、一期功能范围（提醒 + 命令记录器）、交互框架 |
| 2026-08-17 | 《开发前问题清单》 | docs/开发前问题清单.md，18 题，按默认推荐组合执行 |
| 2026-08-17 | 规范文档体系建立 | 需求规范 / 技术规范 / 设计规范 / 开发步骤 四份文档完成 |
| 2026-08-17 | **M0** 环境初始化 | package.json + electron / electron-store 安装完成（95 包，59 秒），空白窗口启动验证通过 |
| 2026-08-17 | **M1** 悬浮宠物窗口 | 透明无边框置顶窗口 + emoji 宠物呼吸动画 + 拖动移动 + 位置记忆 + 点击穿透动态切换 |
| 2026-08-17 | **M2** 左键交互动作 | 300ms 单/双击判定 + 三套 CSS 动作随机播放（跳跃/转圈/摇摆）+ 1.2s 兜底回 idle |
| 2026-08-17 | **M3** 右键气泡 + 命令记录器 | 带箭头气泡 + Tab 栏 + 命令增删改 + 点击复制 + IPC 持久化 |
| 2026-08-17 | **M4** 提醒管理界面 | 提醒组件工厂（气泡 Tab 与双击面板复用）+ 定点/周期 CRUD + 启停开关 + 护眼/时长预置 |
| 2026-08-17 | **M5** 提醒触发与表现 | 主进程 10 秒轮询调度器 + 动画/提示音/气泡三合一 + 8 秒自动消失 |
| 2026-08-17 | **M6** 收尾验收 | 语法校验全通过、启动实测无报错、安全红线核查通过 |
| 2026-08-17 | 双屏支持 | main.js 引入 `screen` 模块：启动位置多屏校验与就近修正（拔副屏不再丢失宠物）+ 跨屏拖动边界限制在鼠标所在屏 workArea 内 |
| 2026-08-17 | 跨屏拖动定位修复 | 拖动由相对增量累加改为绝对锚点方案（drag-start/drag-to/drag-end 三通道，IPC 接口同步更新），消除跨屏漂移与边界突跳；拖拽结束即时落盘位置 |
| 2026-08-17 | 副屏多次拖动不跟随修复 | 拖动边界由“鼠标所在屏 workArea”改为“全部显示器并集包围盒”（显示器变更自动重算），消除边界“钉死”；拖动中锁定穿透切换 + `e.buttons === 0` 兜底结束，修复 mouseup 丢失导致的定位错乱 |
| 2026-08-17 | 混合 DPI 定位修复 | 拖动定位坐标来源由渲染层 e.screenX/Y 改为主进程 `screen.getCursorScreenPoint()`（渲染层只发信号不传坐标），修复副屏回主屏后“只能向下不能向上”；新增窗口失焦强制结束拖动，修复悬停漂移 |
| 2026-08-17 | 拖动向下漂移修复 | 主进程新增逻辑位置账本（lastLogicalPos）：锚点计算与落盘一律以账本为准，拖动全程不读 `win.getPosition()`，切断混合 DPI 下 getPosition/setPosition 往返误差的闭环累积 |
| 2026-08-17 | 长按下沉修复（根因） | 定位到 Electron 远古 bug：Windows 非 100% 缩放下 setPosition 会隐形改变窗口尺寸，宠物为窗口内 bottom 定位而持续下沉；修复为每次 setPosition 后立即 `setSize` 复位尺寸 + 窗口启用 `useContentSize: true` |
| 2026-08-17 | 副屏下沉修复 | 主屏已修复但副屏仍漂移：缩放不同的显示器走不同 DIP 换算路径。改用应用级 `force-device-scale-factor=1` 统一全部屏幕坐标空间 + 拖动改 `setBounds`（位置尺寸一次设定）；旧保存位置作废一次性清空 |
| 2026-08-17 | UI 放大 | 缩放因子统一后整体视觉偏小：窗口 480×400→560×560，宠物 emoji 96px→128px，气泡/面板加宽至 380/400px，全站字号 13px→15px；设计规范/技术规范同步更新 |
| 2026-08-17 | **配置中心（v1.2）** | 双击改为打开大配置框（双 Tab：全部常用命令管理 + 提醒管理）；命令支持跨行长文本（textarea，上限 2000 字符）；每条命令可勾选“显示在右键便捷菜单”至多 5 条；右键气泡常用命令 Tab 仅显示便捷命令（旧数据自动归一化，超出 5 条按顺序降级）；文档（需求 v1.1/设计 v1.2/技术/AGENTS）同步更新 |
| 2026-08-17 | **配置中心优化（v1.3）** | ①双击配置中心**居中放大**：新增 window:set-expanded IPC，展开时窗口 760×720 居中于鼠标所在屏（类似设置窗口）、全窗口可交互、不响应拖动；关闭还原宠物态 560×560 与穿透 ②常用命令顶部新增**标题关键词搜索框**（实时过滤，不区分大小写，无匹配提示） ③右键气泡改为**纯复制看板**：删除 Tab 栏/新增/编辑/删除等全部配置能力，仅保留便捷命令（≤5 条）展示 + 点击复制 + 退出按钮；文档（需求 v1.2/设计 v1.3/技术/AGENTS）同步更新 |
| 2026-08-17 | **配置中心独立窗口（v1.4）** | 用户反馈：双击后弹窗居中但**宠物也跟着居中**。根因：v1.3 是单窗口展开方案，宠物在窗口内 bottom 定位，窗口居中必然带动宠物。改造为**第二 BrowserWindow**（cfgWin 700×680 居中于鼠标所在屏，?mode=config 加载同一 index.html，渲染层双模式分支隐藏宠物/气泡/通知、面板垂直居中）；宠物窗口原位不动；移除 window:set-expanded 展开/恢复机制；配置窗口增删改通过 data:changed 广播实时同步宠物窗口（排除发起者）；关闭方式 ✕/Esc；文档（需求 v1.3/设计 v1.4/技术/AGENTS）同步更新 |
| 2026-08-18 | **应用打包（v1.5）** | 用户要求打包为应用且图标用桌宠猫猫头。①安装 electron-builder 26 并配置 NSIS 安装包（oneClick 关闭/可选目录/桌面快捷方式）②图标链路：`scripts/make-icon.js` 隐藏窗口 capturePage 截取 🐱 emoji（离屏渲染踩坑：屏幕外窗口/offscreen 模式均无帧，最终用 show:false + capturePage）→ assets/icon.png（512×512 透明）→ 打包自动转 ico 嵌入 ③网络踩坑：electron zip 默认走 GitHub 极慢（ETA 62 分钟），配置 `electronDownload.mirror` 指向 npmmirror 后秒过；工具包（winCodeSign-2.6.0/nsis-3.0.4.1/nsis-resources-3.4.1/7zip@1.0.0）经 SHA256 校验手动放入 `%LOCALAPPDATA%\electron-builder\Cache` 归档缓存绕过卡死下载 ④产物：`dist\AI桌宠 Setup 1.0.0.exe`（73.7MB）+ win-unpacked 绿色版，启动实测通过；文档（技术/AGENTS）同步更新 |
| 2026-08-18 | **性能优化（v1.6）** | 用户两项优化：①配置中心可拖动——拖动机制泛化为两窗口共用（drag-start 按 e.sender 识别来源窗口选账本 lastLogicalPos/cfgPos，drag-to 按 dragWin 尺寸与保留可见量设并集边界，配置窗口拖完不落盘下次仍居中）；渲染层按住面板头部（排除 Tab/关闭按钮）发拖动信号，含 buttons/blur 三重兜底 ②复制看板命令**只显示第一行**——bubble 模式取 `content.split('\n')[0]` + single-line 单行省略（悬停可见完整内容，点击仍复制全文）；文档（需求/设计/技术）同步更新 |
| 2026-08-18 | **纯净版安装包（v1.0.1）** | 用户要求：只打包安装包、不带当前配置的常用命令（纯净版）、版本 v1.0.1。①纯净性确认：用户数据存于 electron-store 默认 userData（开发/安装版路径不同），`files` 白名单仅 5 个代码文件，asar 内零用户数据——安装包天然纯净 ②打包遇坑：项目在 D:\Documents 被同步盘/安全软件监控，electron-builder 解压后 unlink/rename asar 持续报 EBUSY/EPERM（重试 6 次无效）——根因是外部进程持续持有 asar 句柄；解法：输出目录改到系统 TEMP（`--config.directories.output=%TEMP%\ai-pet-dist`）一次成功，安装包复制回 dist\；固化为 `scripts/dist-temp.ps1` ③产物：`dist\AI桌宠 Setup 1.0.1.exe`（73.7MB，含 v1.6 全部优化）；旧版 1.0.0 产物与残留 tmp 目录因句柄锁暂无法删除（锁释放后可手动清理） |
| 2026-08-18 | **交互面板改造（v2.0）** | 用户需求变更：右键复制看板在实际体验中没有作用，改为对话交互面板。①右键气泡改为**对话交互面板**——上方聊天消息流 + 底部输入框（宠物头像 🐱 + 输入框 + 发送按钮），首次打开自动显示默认问候语“你好呀，今天过得怎么样？”，后续回复持续用对话气泡追加；大模型 agent 接入前发送后以占位回复代替（sendChat 为后续接入的集中替换点）②**数据模型迁移**：quick（原“显示在右键看板”）→ pinned（置顶），旧数据 quick=true 自动迁移为 pinned=true 并删除 quick 字段，置顶无数量上限 ③**配置中心改造**：命令勾选改为“置顶显示（排在列表最前面）”（📌 徽标 + 置顶排序），“退出宠物”按钮移入配置中心底部；复制看板相关（bubble 模式 / QUICK_MAX / single-line / quick-badge）全部下线 ④文档（需求/设计/技术 v2.0）同步更新 |
| 2026-08-18 | **交互入口对调（v2.1）** | 用户提议双击/右键对调并采纳：**双击打开对话交互面板**（对话是 agent 接入后的高频主功能，双击更自然；顺带优化打开即聚焦输入框，双击后直接打字）、**右键打开配置中心**（符合 Windows“右键=菜单/设置”惯例，配置为低频操作交给刻意手势）；renderer.js 交换 onDoubleClick 与 contextmenu 两个 handler（右键同时收起对话面板）；顺带清理 quick-row 类名→pin-row；文档（需求/设计/技术 v2.1）同步更新 |
| 2026-08-19 | **一键打包脚本（v2.2）** | 用户要求：打包动作固化为脚本/程序，且每次打包不含个人配置。升级 `scripts/dist-temp.ps1` 为四步流水线（清理 TEMP → electron-builder 打包 → **asar 纯净性校验** → 复制回 dist 仅留最新）；新增双击入口 `一键打包.bat` 与 `npm run dist:clean`；校验白名单 6 项（package.json/main/preload/renderer/index/styles），node_modules 生产依赖自动跳过，实测包内零个人数据。踩坑两处：①ps1 无 BOM 时 Windows PowerShell 5.1 按 GBK 解码中文字符串破坏语法（报 exit 未识别）→ 补 UTF-8 BOM 解决 ②校验初版把 node_modules 误报为白名单外 → 顶层文件与依赖树分离校验；文档（技术 v2.2）同步更新 |
| 2026-08-19 | **大模型会话接入（v2.3）** | 用户要求：复用 deepseek-harness 的先进对话能力实现流式陪伴对话（一期无需 agent），**不改动 harness**（后续将删除），预留 .env 填 Key 即用。先出方案文档《会话接入方案》比较四条路径（直连 API/进程桥/ACP/全量集成）→ 选定方案 A（直连 DeepSeek API）复刻 harness 会话设计要点。实施：①`chat.js` 纯 Node 流式后端（SSE 解析/正文思考分离/防御式错误归一/空回复重试/超窗摘要压缩/结构化提示词/历史持久化，零新增依赖——Node 内置 fetch）②`.env` 预留（DEEPSEEK_API_KEY 等，.gitignore 与 files 白名单双重排除，绝不进包/进仓库）③main.js 接入引擎 + 4 个 IPC 通道（chat:send/abort/set-config/clear-history）+ 3 个推送（chat:chunk/done/error）④preload 白名单桥接 ⑤渲染层流式渲染（逐字输出 + reasoner 思考区可折叠 + 打字光标 + 错误态 + 发送中止）⑥配置中心新增「大模型」页签（Key/地址/模型/人设，留空回落 .env，Key 不进渲染层）⑦`scripts/test-chat.js` 单测 7 项全过 + 四文件语法检查 + npm start 冒烟通过；文档（需求/技术 v2.3、方案文档标注已实施）同步更新 |
| 2026-08-19 | **性能与置顶优化（v2.4）** | 用户报告三问题（安装包大/偶发卡顿/置顶失效），先出方案文档《性能与置顶优化方案》定位根因，用户拍板：安装包暂缓，只实施后两个。①**置顶保活**：Windows TOPMOST 无绝对优先级（后来激活的置顶窗口盖住桌宠）→ 创建后提升至 `setAlwaysOnTop(true, 'screen-saver')` 级 + 每 5 秒 `moveTop()` 保活（只调 Z 序不抢焦点）；配置中心 panel-footer 新增「始终置顶」勾选（settings.alwaysOnTop，关闭时降级普通窗口并停止保活——把与用户主动置顶应用的冲突选择权交还用户）；cfgWin 同级别处理；IPC `window:set-always-on-top` + preload 桥接。②**拖动节流**：mousemove 每次鼠标事件发一次 dragTo+setBounds（125-1000Hz 远超 60Hz）→ rAF 节流一帧最多一次，endDrag 统一收尾（取消未发帧）。③**动画减负**：#pet 加 will-change: transform（合成层预建）+ drop-shadow 减淡（.18→.12、blur 6→4px）+ 呼吸动画 3s→5s（降低重绘频率）。三文件语法检查 + npm start 冒烟通过；文档（方案/日志/技术/AGENTS）同步更新 |
| 2026-08-19 | **配置文件机制与回显修复（v2.5）** | 用户报告：安装版配置 Key/人设后“关闭窗口就失效”、找不到配置文件。排查结论：①**回显 bug（根因）**：配置中心「大模型」表单在 loadData 前创建，fill() 读到空 state，已保存值永远不回显——看似丢失实则一直在 config.json 里（经 %APPDATA%\ai-desktop-pet\config.json 验证 systemPrompt 完好）；修法：init 先 await loadData 再建组件 ②**配置文件需求**：打包版首次启动在安装目录（exe 旁）自动生成带注释的 `.env` 模板（DEEPSEEK_API_KEY/BASE_URL/MODEL/SYSTEM_PROMPT，编辑后重启生效；Program Files 不可写时回落 userData）；chat.js resolveConfig 新增 DEEPSEEK_SYSTEM_PROMPT（人设支持 .env）；配置中心 hint 显示 .env 实际路径（data:get-all 新增 envFile 字段）；dist-temp.ps1 附 dist\.env.example 说明 ③**镜像源切换**：npmmirror.com 大文件下载仅 182KB/s 致打包卡 10+ 分钟，改 registry.npmmirror.com（2.26MB/s）；④版本号 2.2.0（衔接用户自打包的 2.1.0）。实测：win-unpacked 启动后 .env 模板成功生成、四文件语法检查、chat 单测 7 项全过、2.2.0 安装包打包纯净校验通过 |
| 2026-08-23 | **九组动作素材切分** | 按用户提供的 `docs/spritesheet.webp` 从上到下等分为 9 行动作组，新增 `scripts/slice-spritesheet.js` 可复用切图脚本，生成 `assets/pet-actions/action-01` 至 `action-09`，每组输出透明 `strip.png`；同时生成 `assets/pet-actions/manifest.json` 记录源图坐标与尺寸，便于后续接入桌宠状态机。本次只做素材落盘，暂不替换运行逻辑。 |
| 2026-08-23 | **九组动作素材替换桌宠本体** | `scripts/slice-spritesheet.js` 升级为透明逐帧切分（9 组共 57 帧：`frame-*.png`）；`index.html` 将 `#pet` 从 emoji 改为图片帧容器；`renderer.js` 状态机按 `idle/action/remind` 播放帧序列（idle=`action-01`，remind=`action-06`，单击随机 `action-04/05/07/08/09`）；`styles.css` 调整角色尺寸与气泡位置；`package.json` 与 `scripts/dist-temp.ps1` 纳入 assets 素材打包；技术/设计规范同步升级 v2.6。 |
| 2026-08-23 | **待机动作节奏优化** | 按用户反馈“播放速度太快、以 action7 作为待机动作、不要连续播放”：`renderer.js` 将 idle 改为 `action-07` 第一帧静止显示，每 20 秒播放一次完整 `action-07`；交互动作从 `action-04/05/08/09` 随机播放并整体放慢帧间隔；提醒动作帧间隔放慢；`styles.css` 将交互/提醒外层动效时长调慢；设计/技术规范同步更新。 |
| 2026-08-23 | **关闭桌宠大小浮动** | 按用户要求关闭大小浮动：`styles.css` 将 idle 的 CSS 呼吸缩放关闭，并移除互动/提醒外层动效中的 `scale()`，仅保留轻微位移；设计/技术规范同步记录。 |
| 2026-08-24 | **点击动作池扩展** | 按用户要求接入剩余动作组：左键单击随机池从 `action-04/05/08/09` 扩展为 `action-01/02/03/04/05/08/09`；`action-07` 仍专用于待机（静止为主，每 20 秒播放一次），`action-06` 仍专用于提醒；设计规范同步更新。 |
| 2026-08-24 | **使用时长提醒语音接入** | 按用户提供的 `assets/audio.wav` 接入“看电脑 1 小时/使用时长”提醒语音：主进程 `reminder:trigger` 附带 `preset/type`，渲染层在 `preset='usage'` 或文本匹配“使用电脑 1 小时/看电脑一小时”时播放 `assets/audio.wav`；播放失败时回退原 WebAudio 双音提示音；普通提醒不受影响。 |
| 2026-08-24 | **默认图标与提醒预置调整（v2.7）** | 将 `action-04/frame-03.png` 等比居中适配为 512×512 透明 `assets/icon.png`，`make-icon` 脚本同步改为可复现该图标；首次启动仅预置 60 分钟使用时长提醒，并精确迁移删除未修改的旧版 45 分钟系统护眼预置，用户新建或改过的护眼提醒保留；应用版本升至 2.2.1。 |
| 2026-08-24 | **透明聊天头像替换（v2.8）** | 使用 imagegen 背景抠除流程处理用户截图：AI 键控版本因重绘主体未采用，最终对原图按边界背景色抠除并裁切为 196×180 透明 `assets/chat-avatar.png`；静态输入区、历史机器人消息、流式机器人回复三处 🐱 全部替换为图片头像；原截图从打包白名单排除；应用版本升至 2.2.2。 |
| 2026-08-24 | **「萤光织界」视觉升级（v2.9）** | 按用户确认移除 `diamond-mark.png`，并重生不含该标识的标题组合图/素材总览；聊天面板与配置中心采用月白半透明卡片，卡片内接入羽翼标识、晶体标识/水印、透明纹理与三色粒子；表单、Tab、按钮、气泡和思考/流式/错误状态统一为青金粉色系；两个窗口的卡片外围保持完全透明；应用版本升至 2.2.3。 |
| 2026-08-24 | **v2.9 黑色外框修正** | 根据实机反馈移除聊天卡片的 `chat-night.png` 深色底和配置窗口 `body` 上的 `config-steel.png` 整页背景，同时取消 `#stage` 全屏纹理层；聊天改为月白半透明卡片，配置窗口恢复 alpha 透明，卡片阴影同步减淡。 |
| 2026-08-25 | **配置窗口宠物形象闪现修复** | 右键打开配置中心时宠物形象会闪现一瞬。根因两处：①cfgWin 创建无 `show:false`，创建即显示加载过程 ②v2.5 回显修复把 `await loadData()` 提到 init 最前，连带把隐藏宠物/气泡/通知的代码推迟到 IPC 往返之后。修复：renderer.js 在脚本顶层（isConfigWindow 判断处）同步隐藏三元素，不等 loadData；main.js cfgWin 加 `show:false` + `ready-to-show` 后再显示。两文件语法检查通过。 |

## 🔄 进行中事项

| 事项 | 开始时间 | 备注 |
|---|---|---|
| （暂无） | - | v1.0.1 纯净版安装包完毕 |

## 📋 待办事项

- [x] **M0** 环境初始化
- [x] **M1** 悬浮宠物窗口
- [x] **M2** 左键交互动作
- [x] **M3** 右键会话气泡 + 命令记录器
- [x] **M4** 提醒管理界面
- [x] **M5** 提醒触发与表现
- [x] **M6** 收尾打磨与总验收
- [x] **配置中心（v1.2）** 双击大配置框 + 跨行长命令 + 便捷菜单勾选（≤5 条）
- [x] **配置中心优化（v1.3）** 居中放大设置窗口 + 标题搜索 + 右键纯复制看板
- [x] **配置中心独立窗口（v1.4）** 双击只弹居中设置窗口，宠物原位不动；配置改动实时同步看板
- [x] **应用打包（v1.5）** electron-builder NSIS 安装包 + 猫猫头图标 + 绿色版
- [x] **性能优化（v1.6）** 配置中心头部可拖动 + 复制看板命令只显示第一行
- [x] **纯净版安装包（v1.0.1）** 不含任何用户数据，仅安装包形式发布
- [x] **交互面板改造（v2.0）** 右键改对话交互面板（问候语 + 气泡流 + 输入框，agent 接入占位）；quick→pinned 置顶迁移；退出按钮移入配置中心
- [x] **交互入口对调（v2.1）** 双击打开对话交互面板（聚焦输入框）、右键打开配置中心
- [x] **一键打包脚本（v2.2）** 四步流水线（TEMP 打包 + asar 纯净校验 + dist 回收），双击 bat / npm run dist:clean 入口
- [x] **大模型会话接入（v2.3）** chat.js 流式后端（SSE 解析/思考分离/摘要压缩/错误归一）+ .env 填 Key 即用 + 渲染层流式气泡 + 「大模型」配置页签 + 单测 7 项全过
- [x] **三大问题优化（v2.4，安装包项暂缓）** 见 docs/性能与置顶优化方案.md：①置顶保活（screen-saver 级 + 5 秒 moveTop + 配置中心「始终置顶」开关）②拖动 rAF 节流 + will-change + 呼吸 5s + 阴影减负；③安装包体积优化（electronLanguages/NSIS 压缩）按用户指示暂缓
- [x] **配置文件机制与回显修复（v2.5）** ①修复配置中心「大模型」表单不回显已保存值的 bug（init 先 loadData 再建组件）②打包版首次启动在安装目录自动生成 .env 配置模板（Key/地址/模型/人设，含注释），配置中心 hint 显示 .env 路径 ③electron 镜像源切 registry.npmmirror.com（下载快 12 倍）④产物 dist\AI桌宠 Setup 2.2.0.exe
- [ ] （后续）打开本地文件 / 命令搜索（全文/模糊）/ 命令执行
- [x] **九组动作素材替换桌宠本体** `docs/spritesheet.webp` → `assets/pet-actions/` 透明逐帧 PNG；运行时用图片帧动画替换 emoji 宠物
- [x] **默认图标与提醒预置调整（v2.7）** 使用 `action-04/frame-03.png` 作为打包图标；移除默认 45 分钟护眼预置
- [x] **透明聊天头像替换（v2.8）** 抠除用户截图黑底，生成透明 PNG 并替换聊天区全部 🐱 头像
- [x] **「萤光织界」视觉升级（v2.9）** 聊天/配置页面接入角色主题素材，移除 diamond-mark，统一青金粉与深钢蓝视觉系统
- [ ] **v2.9 本机视觉复验** 在正常 Windows 桌面环境运行 `npm start`，确认双击聊天面板/右键配置中心的背景、可读性和窗口性能
- [ ] （后续可选）进一步精修动作语义映射 / 调整帧率 / 接入更多状态（睡眠、开心、拖拽等）

---

## 📜 变更记录

| 日期 | 里程碑 | 变更内容 | 结果 |
|---|---|---|---|
| 2026-08-17 | - | 项目规划阶段：建立文档体系与开发日志机制 | ✅ 完成 |
| 2026-08-17 | M0 | package.json 创建，npm install（electron 走 npmmirror 镜像），空白窗口启动验证 | ✅ 通过 |
| 2026-08-17 | M1-M2 | main.js（窗口/IPC/调度器）、preload.js、styles.css 宠物与动画实现 | ✅ 通过 |
| 2026-08-17 | M3-M5 | index.html 结构 + renderer.js（气泡/记录器/提醒组件/触发表现）实现 | ✅ 通过 |
| 2026-08-17 | 规范变更 | 技术规范/设计规范升级 v1.1：素材 GIF → emoji + CSS 动画；mp3 → WebAudio 合成音 | ✅ 已修订 |
| 2026-08-17 | M6 | `node --check` 三文件语法通过；`npm start` 实测启动无报错 | ✅ 通过 |
| 2026-08-17 | 双屏支持 | 位置记忆升级为多屏适配（`screen` 模块校验/就近吸附/跨屏拖动限制）；技术规范 §3 同步更新 | ✅ 通过 |
| 2026-08-17 | 跨屏定位修复 | 拖动重构为绝对锚点方案，修复跨屏后定位漂移/突跳；`move-by` IPC 替换为 `drag-start/drag-to/drag-end`；启动实测无报错 | ✅ 通过 |
| 2026-08-17 | 拖动跟随修复 | 边界改屏幕并集包围盒（不钉边界）；拖动中锁定穿透切换 + mouseup 丢失兜底；启动实测无报错 | ✅ 通过 |
| 2026-08-17 | 混合 DPI 定位修复 | 坐标来源改主进程 `getCursorScreenPoint`；失焦/按键兜底双重防状态残留；启动实测无报错 | ✅ 通过 |
| 2026-08-17 | 向下漂移修复 | 逻辑位置账本切断 getPosition 往返误差闭环；启动实测无报错 | ✅ 通过 |
| 2026-08-17 | 长按下沉修复 | setPosition 后 setSize 复位尺寸 + useContentSize:true，斩断隐形尺寸漂移；启动实测无报错 | ✅ 通过 |
| 2026-08-17 | 副屏下沉修复 | force-device-scale-factor=1 统一坐标空间 + setBounds 拖动；启动实测无报错 | ✅ 通过 |
| 2026-08-17 | 配置中心 | 命令组件工厂（气泡/面板复用）+ quick 字段归一化；`node --check` 通过；启动实测无报错 | ✅ 通过 |
| 2026-08-17 | 配置中心优化 | 窗口展开/恢复 IPC + 搜索过滤 + 看板精简；`node --check` 通过；启动实测无报错 | ✅ 通过 |
| 2026-08-17 | 配置中心独立窗口 | 第二 BrowserWindow 承载配置中心（宠物窗口不动）+ 数据变更广播同步；`node --check` 通过；启动实测无报错 | ✅ 通过 |
| 2026-08-18 | 应用打包 | electron-builder NSIS 安装包 + emoji 截图图标；镜像配置后打包成功，Setup 与绿色版启动实测通过 | ✅ 通过 |
| 2026-08-18 | 交互面板改造 | 复制看板→对话交互面板（问候语/气泡流/输入框/占位回复）；quick→pinned 数据迁移；置顶排序 + 退出按钮移入配置中心；`node --check` 通过；启动实测无报错 | ✅ 通过 |
| 2026-08-18 | 交互入口对调 | 双击=对话交互面板（聚焦输入框）、右键=配置中心；handler 对调 + 类名语义清理；启动实测无报错 | ✅ 通过 |
| 2026-08-19 | 一键打包脚本 | 四步流水线（TEMP 打包 + asar 纯净校验 + dist 回收）；双击 bat 实测打包成功（73.7MB），校验通过零个人数据 | ✅ 通过 |
| 2026-08-23 | 九组动作素材切分 | `docs/spritesheet.webp` 按 9 行切为 `assets/pet-actions/action-01` 至 `action-09`，每组 `strip.png` 尺寸均为 1536×208；抽看首尾两行动作条正常 | ✅ 通过 |
| 2026-08-23 | 九组动作素材替换 | 生成 57 张透明逐帧 PNG，替换 `#pet` 为图片帧容器，状态机播放新素材；打包白名单同步包含 assets | ✅ 通过 |
| 2026-08-23 | 待机动作节奏优化 | idle=`action-07`，静止为主，每 20 秒播放一次；交互/提醒帧率与外层 CSS 动效放慢 | ✅ 通过 |
| 2026-08-23 | 关闭大小浮动 | idle 不再执行 breathe 缩放；互动/提醒动效移除 scale，角色尺寸保持稳定 | ✅ 通过 |
| 2026-08-24 | 点击动作池扩展 | 左键单击随机播放 `action-01/02/03/04/05/08/09`，待机/提醒动作保持专用 | ✅ 通过 |
| 2026-08-24 | 使用时长提醒语音接入 | `assets/audio.wav` 用于看电脑 1 小时提醒；普通提醒继续使用合成双音；语音失败自动回退 | ✅ 通过 |
| 2026-08-24 | 默认图标与提醒预置调整 | 新图生成并验证为 512×512 透明 PNG；默认数据与精确旧预置迁移完成；语法检查、7 项会话单测、8 秒启动冒烟均通过 | ✅ 通过 |
| 2026-08-24 | 透明聊天头像替换 | 头像为 196×180 RGBA PNG、四角 alpha=0；代码无 🐱 残留；语法检查、7 项会话单测、8 秒启动冒烟均通过 | ✅ 通过 |
| 2026-08-24 | 聊天/配置页面视觉升级方案 | 从 `assets/pet-actions` 提取色彩、晶体、羽翼、菱形与机甲结构语言，形成「萤光织界」视觉方向；输出 10 页横版 PDF（元素板、设计系统、双页面概念稿、素材包与验收口径）；本次仅产出方案，未修改应用代码 | ✅ 完成 |
| 2026-08-24 | 「萤光织界」视觉素材提取 | 新增 `assets/ui-theme/firefly/`：聊天/配置背景各 1 张、羽饰/菱形/胸饰/标题装饰、透明菱形纹理、青金粉粒子双尺寸及总览图；逐文件校验尺寸、RGBA 透明角与 PNG 完整性；尚未接入页面代码 | ⏳ 待用户确认 |
| 2026-08-24 | 「萤光织界」视觉升级实施 | 修订需求/设计/技术规范至 v2.9；移除 diamond-mark 并更新素材总览/清单；HTML/CSS 接入羽饰、晶体、纹理与粒子，根据实机反馈取消两张深色运行时背景，确保卡片外围透明；素材引用、manifest JSON、四文件语法检查通过，会话单测 7/7 通过；Electron 启动因 Codex 受管环境 GPU 子进程 `0xC0000135` 无法完成，留待本机视觉复验 | ⚠️ 实现完成，待本机复验 |
| 2026-08-25 | 配置窗口闪现修复 | 配置中心打开时宠物形象闪现一瞬：渲染层隐藏逻辑被 v2.5 的 `await loadData()` 推迟到 IPC 往返之后 + cfgWin 无 `show:false`；修复为脚本顶层同步隐藏 + `ready-to-show` 后显示；`node --check` 两文件通过 | ✅ 通过 |

---

## ⚠️ 问题与风险记录

| 日期 | 问题描述 | 处理方案 | 状态 |
|---|---|---|---|
| 2026-08-17 | 问题清单 18 题用户尚未逐题确认 | 按默认推荐组合执行，用户确认后如有变更同步更新规范文档 | 观察中 |
| 2026-08-17 | 像素 GIF 素材透明背景难保证、制作耗时 | 规范变更 v1.1：改用 emoji + CSS 动画，二期可替换 | ✅ 已解决 |
| 2026-08-17 | 穿透窗口中"点击桌面空白关闭气泡"无法捕获窗口外点击 | 实现为窗口内空白区域兜底关闭 + ✕ 按钮，已在代码注释说明 | ⚠️ 已知限制 |
| 2026-08-17 | 提醒触发需真实等待才能验证（当前仅默认 60 分钟使用时长提醒） | 调度逻辑经代码走查确认；验收时可将间隔临时改为 1 分钟实测 | 💡 建议课堂演示前临时调短 |

---

## 📝 M6 总验收结论（对照需求规范 §6）

| 验收项 | 结果 |
|---|---|
| 宠物悬浮桌面最顶层、背景完全透明 | ✅ transparent + alwaysOnTop + 全链路无底色 |
| 关闭重开后提醒与命令数据保留 | ✅ electron-store 持久化，位置同样记忆 |
| 定点/周期/时长三类提醒可触发并三合一呈现 | ✅ 调度器 + 动画/声音/气泡链路完整 |
| 无崩溃、无明显卡顿 | ✅ 启动实测无报错；渲染层仅轻量 CSS 动画 |
| 安全红线（contextIsolation / textContent / 无命令执行） | ✅ preload 白名单 + DOM 安全渲染，无 child_process |

**二期建议**：① 素材升级（GIF/Live2D，保留状态机 class 约定即可无缝替换）② 气泡追加“搜索”Tab ③ `shell.openPath` 打开文件 ④ 开机自启（app.setLoginItemSettings）。

## 📝 配置中心（v1.2）验收（对照需求规范 §2.4）

| 验收项 | 结果 |
|---|---|
| 双击宠物打开大配置框（替代原提醒面板） | ✅ 配置中心双 Tab（常用命令/提醒管理），面板 480×520 |
| 全部命令管理 + 跨行长文本 | ✅ textarea 输入（可纵向拉高，上限 2000 字符），列表多行完整显示 |
| 便捷菜单勾选至多 5 条 | ✅ 配置中心列表项/表单双处勾选，超出拒绝并提示；旧数据超 5 条自动降级 |
| 右键气泡仅显示便捷命令 | ✅ 气泡只渲染 quick=true（≤5 条），带“便捷”徽标；交互方式不变 |
| 提醒管理入口保留 | ✅ 气泡与配置中心均有提醒管理 Tab（组件工厂复用） |

## 📝 配置中心优化（v1.3）验收（对照需求规范 §2.4/§2.5）

| 验收项 | 结果 |
|---|---|
| 双击配置中心居中放大（类似设置窗口） | ✅ 窗口展开态 760×720 居中于鼠标所在屏；关闭还原宠物态与穿透；展开态不响应拖动 |
| 常用命令标题关键词搜索 | ✅ 搜索框实时过滤（不区分大小写），无匹配显示空状态 |
| 右键区域删除可配置能力 | ✅ 右键看板无 Tab/新增/编辑/删除，仅便捷命令展示 + 点击复制 + 退出按钮 |
| 提醒管理入口不丢失 | ✅ 提醒管理为配置中心 Tab（右键区域不再提供） |

## 📝 配置中心独立窗口（v1.4）验收（对照需求规范 §2.4）

| 验收项 | 结果 |
|---|---|
| 双击只弹居中设置窗口，宠物原位不动 | ✅ 配置中心为独立 BrowserWindow（700×680）居中于鼠标所在屏；宠物窗口位置/尺寸/穿透均不变化 |
| 配置窗口形态完整（双 Tab + 搜索 + 管理） | ✅ 同一 index.html 以 ?mode=config 加载，渲染层隐藏宠物/气泡/通知并居中铺开面板 |
| 配置改动实时同步右键看板 | ✅ 增删改后主进程广播 data:changed，宠物窗口重新拉取并刷新 |
| 关闭方式自然 | ✅ ✕ 按钮或 Esc 关闭配置窗口；宠物窗口无还原问题（从未移动） |

## 📝 交互面板改造（v2.0）验收（对照需求规范 §2.4/§2.5）

| 验收项 | 结果 |
|---|---|
| 右键弹出对话交互面板（替代复制看板） | ✅ 上方聊天消息流 + 底部输入区（宠物头像 🐱 + 输入框 + 发送按钮）；输入框支持回车/点击发送（上限 500 字符） |
| 首次打开显示默认问候语 | ✅ “你好呀，今天过得怎么样？”以 bot 气泡（带 🐱 头像）自动输出 |
| 后续回复持续用对话气泡追加 | ✅ 会话期间消息流保留（气泡区滚动，重开不清空）；用户消息右对齐蓝色气泡 |
| 大模型 agent 接入占位 | ✅ sendChat 为集中接入点，接入前发送后返回占位回复并明确提示 |
| 命令勾选改为置顶 | ✅ quick→pinned 迁移（旧数据自动保留勾选状态）；置顶条目带 📌 徽标排在列表最前；无数量上限 |
| 退出按钮移入配置中心 | ✅ 配置中心底部“退出宠物”按钮；右键交互面板不再有退出入口 |

## 📝 交互入口对调（v2.1）验收（对照需求规范 §3）

| 验收项 | 结果 |
|---|---|
| 双击宠物打开对话交互面板 | ✅ onDoubleClick 改为 toggleBubble；首次打开显示问候语，打开即聚焦输入框（双击后直接打字） |
| 右键宠物打开配置中心 | ✅ contextmenu 改为 api.setConfigOpen(true)，同时收起对话面板；配置窗口居中于鼠标所在屏 |
| 单击动作不受影响 | ✅ 单击/双击 300ms 判定窗口不变，单击仍播放随机动作 |

## 📝 大模型会话接入（v2.3）验收（对照需求规范 §2.5）

| 验收项 | 结果 |
|---|---|
| .env 填 Key 即用 | ✅ 根目录 `.env` 填 `DEEPSEEK_API_KEY` 重启即用；.gitignore 与 files 白名单双重排除（不进仓库/不进安装包） |
| 流式陪伴对话 | ✅ 发送后主进程 chat.js 引擎流式生成，渲染层逐字渲染（带打字光标）；deepseek-reasoner 思考内容进入可折叠思考区 |
| 多轮历史与摘要压缩 | ✅ 历史默认保留 12 轮（思考不入历史），超出自动用大模型生成摘要压缩；重启可续聊；可清空历史 |
| 防御式错误处理 | ✅ 无 Key/超窗/限流/空回复归一为友好提示；空回复自动重试一次；面板关闭/重发自动中止未完成请求 |
| 「大模型」配置页签 | ✅ 配置中心第三页签：Key（password）/地址/模型/人设 + 保存/清空历史；留空回落 .env；Key 只存主进程不进渲染层 |
| 不改动 harness | ✅ deepseek-harness 零改动（仅参考设计，.gitignore 排除，后续可随时删除） |
| 验证 | ✅ 四文件 node --check 全过；`npm run test:chat` 单测 7 项全过；npm start 冒烟无报错 |
