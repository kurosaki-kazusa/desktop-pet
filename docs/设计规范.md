# 设计规范 · AI 悬浮桌面宠物

> 版本：v2.1 ｜ 覆盖：视觉风格、宠物状态与动画、交互面板/配置中心 UI、音效、文案
> v2.1 变更（2026-08-18）：交互入口对调——**双击打开交互面板**（对话是高频主功能，双击更自然，打开即聚焦输入框）、**右键打开配置中心**（符合 Windows“右键=菜单/设置”惯例）
> v2.0 变更（2026-08-18）：右键**复制看板下线**，改为**交互面板**（对话气泡 + 输入框，默认问候语，后续接大模型 agent）；命令置顶勾选（勾选排最前）；“退出宠物”移入配置中心底部
> v1.6 变更（2026-08-18）：配置中心**头部可拖动**（不记忆位置）；复制看板命令**只显示第一行**（单行省略，悬停可见完整内容）
> v1.4 变更（2026-08-17）：配置中心改为**独立设置窗口**（700×680 居中，宠物窗口原位不动）；此前双击会让宠物随窗口一起居中移动，现分离
> v1.3 变更（2026-08-17）：配置中心**居中放大**为设置窗口形态（面板 640×640，标题搜索框）；右键气泡改为**纯复制看板**（去 Tab/新增/编辑，仅展示+复制）
> v1.2 变更（2026-08-17）：双击面板升级为**配置中心**（全部命令管理 + 便捷菜单勾选，命令支持跨行长文本）
> v1.1 变更（2026-08-17）：宠物素材由像素 GIF 改为 **emoji + CSS 动画**；提示音改为 **WebAudio 合成**（见 §2、§5、§7）

---

## 1. 整体视觉风格

- **像素风 + 柔和圆角**：宠物为像素风 GIF，UI 面板采用圆角卡片、柔和阴影，两者对比形成"萌 + 干净"的观感
- **主色**：宠物暖色系（素材决定）；UI 中性浅色底 `#FFFFFFF2`（近白微透明），主强调色 `#5B8DEF`（蓝），提醒警示色 `#FF7A59`（橙红）
- **字体**：系统默认（`"Microsoft YaHei", sans-serif`），正文 15px，标题 16px 加粗
- **透明原则**：窗口除宠物像素与 UI 卡片外必须完全透明，禁止出现任何矩形底色残留

## 2. 宠物状态机与动画

| 状态 | 触发 | 实现 | 行为 |
|---|---|---|---|
| `idle` 待机 | 默认状态 | emoji 🐱 + CSS 呼吸动画（缩放微浮动，3s 循环） | 持续播放 |
| `action` 交互 | 左键单击 | 三套 CSS 动作随机：跳跃 / 转圈 / 摇摆 | 播放约 1.2s 后自动回到 `idle` |
| `remind` 提醒 | 提醒触发 | CSS 弹跳动画 + 头顶提醒气泡 | 气泡关闭后回到 `idle` |

规则：
- 状态切换必须保证回到 `idle` 的兜底路径（动画播完 / 气泡关闭 / 超时 10 秒），不允许卡死在中间态
- 单击事件需与双击区分：使用 300ms 判定窗口（先判定是否双击，超时才执行单击动作），避免双击时误播两次交互动作
- 宠物显示尺寸：约 **128×128px**，位于窗口底部居中

## 3. 交互面板（双击触发，v2.0 取代原复制看板，v2.1 与配置中心入口对调）

- **定位**：双击宠物弹出的**对话交互面板**——上方回复气泡 + 底部输入框（后续接入大模型 agent 后实现真实对话）；打开即聚焦输入框，双击后可直接打字
- **造型**：圆角卡片 + 底部指向宠物的小三角箭头，宽度 380px；对话区最大高度约 220px（内容超出滚动），总高不超过 400px
- **结构**：标题“💬 和桌宠聊天” + 关闭按钮 + 对话区（聊天消息流）+ 输入区（宠物头像 🐱 + 输入框 + 发送按钮）
- **对话气泡**：宠物回复为左侧浅蓝气泡（带 🐱 头像），用户消息为右侧蓝色气泡（右对齐）；首次打开自动显示问候语“你好呀，今天过得怎么样？”
- **输入**：输入框支持回车或点“发送”提交（上限 500 字符）；大模型接入前，发送后以占位回复代替
- **关闭**：点击面板右上角 ✕，或点击宠物/面板以外的区域（依赖点击穿透策略，失焦即关闭）；关闭期间对话记录保留，重开不重置

## 3.5 配置中心（右键触发，v1.4 更新：独立窗口居中，宠物不动；v2.1 入口由双击改为右键）

- **定位**：右键宠物打开的**独立设置窗口**（第二个 BrowserWindow，700×680）——居中于鼠标所在屏，宠物窗口保持原位不动；全窗口可交互（无穿透）
- **拖动**：按住面板**头部标题区域**可拖动窗口到任意位置（Tab 栏与关闭按钮除外；不记忆位置，下次打开仍居中）
- **造型**：圆角大卡片，宽度 640px，最大高度约 648px，顶部标题“⚙️ 配置中心” + 关闭按钮；窗口内垂直居中
- **关闭**：点右上角 ✕ 或按 Esc（窗口不复用宠物态窗口，无还原问题）
- **结构**：顶部 Tab 栏（`常用命令` / `提醒管理`）+ 底部“退出宠物”按钮（v2.0 从右键看板移入）
- **常用命令 Tab**（管理全部命令）：
  - 顶部**搜索框**：按命令标题关键词实时过滤（不区分大小写）；无匹配时显示空状态提示
  - 列表项 = 标题（置顶条目前加 📌）+ 命令内容（多行完整显示，点击复制）+ **置顶勾选**（右侧 checkbox，提示“置顶显示（排在列表最前面）”，无数量上限）+ 编辑/删除
  - 勾选置顶的条目自动排在列表最前（保持各自原有相对顺序）
  - 新增表单：标题 + **多行文本域**（textarea，可纵向拉高，支持跨行长文本/脚本，上限 2000 字符）+ 置顶勾选
- **提醒管理 Tab**：提醒管理唯一入口
- **关闭**：点击右上角 ✕，或点击宠物/面板以外的区域

## 4. 提醒气泡（提醒触发时）

- 从宠物头顶冒出，圆角小卡片，含：提醒图标 + 文本 + "知道了"按钮
- 自动停留 **8 秒**后淡出；点击"知道了"立即关闭
- 出现时伴随宠物 `remind` 动画与提示音

## 5. 音效

- 统一提示音：**WebAudio 合成的双音提示音**（正弦波 A5→D6 两音，总时长 <0.5 秒，v1.1 变更，原 mp3 文件），所有提醒类型共用
- 音量默认 0.8，存于 `settings.volume`（一期不做音量调节 UI）

## 6. 动效与过渡

- 气泡/面板出现：`opacity + translateY(4px)` 淡入，150ms `ease-out`
- 消失：淡出 120ms
- 禁止任何超过 300ms 的动效，保持轻快感

## 7. 素材方案（v1.1 · emoji + CSS 动画）

宠物本体为 🐱 emoji（`#pet`，128px），所有动画由 CSS 关键帧实现，不依赖任何图片素材：

| 状态 | 动画 | 说明 |
|---|---|---|
| 待机 | `breathe` 3s 循环 | 呼吸感（scale + 轻微位移） |
| 交互 | `jump` / `spin` / `wiggle` | 左键单击随机选一套，1.2s 后回 idle |
| 提醒 | `bounce` | 弹跳，提醒气泡关闭后回 idle |

选型理由：emoji 天然透明背景、零素材制作成本，规避 GIF 透明底风险，适合 2 小时内完成的约束。

预留替换路径（二期可选，状态机逻辑无需改动）：

1. 新建 `assets/` 文件夹，放入透明背景的 GIF/PNG 素材（按状态命名：idle / action / remind）
2. `renderer.js` 的 `setPetState()` 中按状态切换 `<img>` 的 src，CSS 动画可叠加保留

音效同理：如后续需要真实音效文件，在 `assets/` 目录新增并替换 WebAudio 实现即可。
